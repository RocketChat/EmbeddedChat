import { css } from '@emotion/react';

export const getRoomStyles = () => {
  const styles = {
    container: css`
      padding: 0 1rem 1rem;
      margin: 0 auto;
      overflow: auto;
    `,
  };
  return styles;
};
